# Generate with: openssl rand -base64 32
NOTIFICATION_ENCRYPTION_KEY=
# Long random bearer token used only by the event producer / scheduler.
NOTIFICATION_INTERNAL_TOKEN=
NOTIFICATION_FROM_EMAIL=Choice Compass <alerts@example.com>
RESEND_API_KEY=
